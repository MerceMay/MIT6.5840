# 分布式强一致性 KV 存储引擎（基于 Raft）

## 项目概述
基于 MIT 6.5840 分布式系统课程，独立实现了一个**高可用、强一致性的分布式键值存储引擎**。项目完整实现了 Raft 共识协议，包括 Leader 选举、日志复制、快照机制等核心功能，并在此基础上构建了支持版本控制的 KV 存储服务。

---

## 简历核心要点（3条）

### 第一点：核心算法——Leader 选举与日志一致性保障

**实现了完整的 Raft 共识协议，重点解决了分布式系统中的 Leader 选举和日志一致性问题：**

1. **选举安全性保障**
   - 采用**随机化选举超时机制**（250-650ms 随机区间），有效避免选举活锁（Split Vote）问题
   - 实现了严格的**日志比较规则**（先比较 Term，再比较 Index），确保只有拥有最新日志的节点才能当选 Leader
   - 通过**任期号单调递增机制**和 votedFor 持久化，防止同一任期内重复投票导致的脑裂问题

2. **日志一致性保障**
   - 实现了 **AppendEntries RPC** 的日志一致性检查机制：通过 `prevLogIndex` 和 `prevLogTerm` 验证日志前缀匹配
   - 针对日志冲突场景，实现了**快速回退优化**：返回 `ConflictTerm` 和 `ConflictIndex`，使 Leader 能够按 Term 批量跳过冲突日志，将回退次数从 O(n) 优化至 O(Term 数量)
   - 严格遵循**只提交当前任期日志**的原则，解决了 Figure 8 中描述的已提交日志被覆盖的 Corner Case

3. **并发模型设计**
   - 采用**单一互斥锁 + 条件变量**的并发模型，通过 `sync.Cond` 实现 applier 协程的高效唤醒
   - 所有 RPC 回调函数在处理前进行**任期和身份双重校验**，确保状态一致性

---

### 第二点：高可用与容错——网络分区恢复与快照机制

**实现了完善的容错机制，确保系统在网络分区、节点宕机等异常场景下的数据一致性和服务可用性：**

1. **网络分区容错**
   - 实现了**心跳机制**（100ms 间隔）与**选举超时检测**，确保网络分区后旧 Leader 能够及时感知并降级为 Follower
   - 通过 RPC 响应中的 Term 比较，实现了**任期发现机制**：任何节点收到更高任期的消息时立即转变为 Follower，防止脑裂后的双 Leader 问题
   - 采用**幂等性设计**：InstallSnapshot 和 AppendEntries RPC 都支持重复调用，确保网络不稳定时的正确性

2. **快照（Snapshot）与 WAL 持久化**
   - 实现了**增量快照机制**：当 Raft 状态大小超过阈值的 90% 时自动触发快照，平衡存储空间和恢复时间
   - 采用 **Write-Ahead Logging** 策略：`currentTerm`、`votedFor`、`log[]` 在每次修改后立即持久化，确保崩溃恢复后的状态一致性
   - 实现了 **InstallSnapshot RPC**：支持 Leader 向落后 Follower 直接发送完整快照，解决了日志压缩后无法同步的问题

3. **崩溃恢复机制**
   - 节点重启时通过 `readPersist()` 恢复持久化状态，并根据 `lastIncludedIndex` 重建日志索引
   - RSM 层实现了状态机快照的独立恢复，确保 KV 存储状态与 Raft 日志的一致性

---

### 第三点：性能优化与工程落地——并发模型与锁竞争优化

**在保证强一致性的前提下，进行了多项性能优化和工程化改进：**

1. **即时日志复制优化**
   - 实现了**日志追加即触发心跳**机制：`Start()` 函数在追加新日志后立即调用 `sendHeartbeatAtOnce()`，无需等待下一个心跳周期，显著降低了客户端请求延迟
   - 采用**并行 RPC 发送**：Leader 向所有 Follower 并行发送 AppendEntries，利用 Go 协程实现高并发

2. **锁竞争优化**
   - **细粒度锁设计**：KV 存储层为每个 Key 维护独立的读写锁（`sync.RWMutex`），Get 操作使用读锁，Put 操作使用写锁，最大化读并发
   - **锁持有时间最小化**：RPC 处理函数在发送 RPC 前释放锁，RPC 完成后重新获取锁并校验状态，避免网络延迟导致的锁长时间持有
   - **异步 Apply 机制**：通过独立的 applier 协程和条件变量，将日志应用与主处理流程解耦，减少锁竞争

3. **RSM 状态机复制层设计**
   - 抽象出**通用的 RSM（Replicated State Machine）框架**，支持任意状态机（如 KV 存储）接入
   - 实现了**操作唯一标识机制**（OpId），配合 pendingOps Map 实现请求的精确匹配和结果回传
   - 采用**超时重试与领导者感知**：客户端通过 100ms 超时轮询检测领导者变更，结合 `lastLeaderId` 缓存优化，减少领导者发现延迟

4. **Channel 与定时器优化**
   - 使用**缓冲 Channel**（容量为 1）处理定时器事件，避免协程阻塞和事件丢失
   - 定时器重置采用**先 Stop 再 Reset** 的安全模式，防止定时器泄漏

---

## 技术栈
- **编程语言**：Go
- **共识协议**：Raft（完整实现 Leader 选举、日志复制、快照）
- **并发原语**：sync.Mutex、sync.RWMutex、sync.Cond、Channel
- **持久化**：WAL（Write-Ahead Logging）+ Snapshot
- **RPC 框架**：基于 Go 的 net/rpc 实现

---

## 核心代码模块
- `raft1/raft.go`：Raft 共识协议核心实现（886 行）
- `kvraft1/server.go`：KV 存储服务层
- `kvraft1/rsm/rsm.go`：RSM 状态机复制框架
- `kvraft1/client.go`：客户端请求路由与重试逻辑
